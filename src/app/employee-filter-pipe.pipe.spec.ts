import { EmployeeFilterPipePipe } from './employee-filter-pipe.pipe';

describe('EmployeeFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new EmployeeFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
